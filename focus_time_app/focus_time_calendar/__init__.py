"""
Implements calendar-specific functionality.

Note: the module should have been named just "calendar", but that caused problems with Python's import-mechanism,
because there is already a "calendar" module in Python's stdlib.
"""
